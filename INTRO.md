# 智能在线考试系统 — 项目介绍与需求总览

> 本文档面向所有需要了解本项目的人：介绍项目定位、系统构成、项目结构与完整的功能需求。
> 快速启动见 [`README.md`](README.md)，架构与实现细节见 [`PROJECT.md`](PROJECT.md)。

---

## 一、项目概述

### 1.1 项目定位

一套**前后端分离的智能在线考试系统**，面向**行测（行政职业能力测验）**等标准化机考场景。管理员在线维护题库、组卷并发布考试；学生在线浏览题库、参加考试；系统交卷即自动判分，成绩、明细与排行实时可查。

试卷**仅支持单项选择题**，与行测全单选的实际情况一致。

### 1.2 「智能」体现在哪

| 能力 | 实现方式 | 特点 |
| :--- | :--- | :--- |
| AI 生成题目 | 调用 DeepSeek 大模型，按知识点/难度/数量出题 | 出题效率从"逐道手录"变为"批量生成 + 人工勾选" |
| 智能组卷 | 规则随机抽题算法（分类 + 难度 + 数量） | 确定性算法，零成本零延迟，抽不足自动放宽难度 |
| 自动判分 | 标准答案逐题匹配 | 交卷瞬间出分，无需人工阅卷 |

### 1.3 用户角色

| 角色 | role | 使用端 | 职责 |
| :--- | :--- | :--- | :--- |
| 管理员 | 0 | 管理后台 | 内容管理（轮播/公告/分类/题目）、组卷发布、考试记录管理、数据概览 |
| 学生 | 1 | 学生前台 | 浏览公告与题库、参加考试、查看成绩与排行 |

登录成功后按角色自动进入对应端；两端路由与权限完全隔离，学生无法访问任何管理功能。

---

## 二、系统构成与技术栈

```
浏览器（Vue 3 SPA）
   │  HTTP /api/v1/**（开发期 Vite 代理，无跨域）
   ▼
Spring Boot 后端（JWT 鉴权 → Controller → Service → MyBatis）
   │                                │
   ▼                                ▼
MySQL 8.0（7 张表）          DeepSeek API（仅 AI 出题）
本地磁盘（轮播图图片，/upload/** 静态映射）
```

| 端 | 技术 |
| :--- | :--- |
| 前端 | Vue 3（Composition API）、Element Plus、Vue Router、Pinia、Axios、Vite |
| 后端 | Java 21、Spring Boot 4.1.1、MyBatis、MySQL 8.0、JWT（jjwt）、BCrypt、EasyExcel、Knife4j |
| AI | DeepSeek（OpenAI 兼容协议，RestClient 直调，无 SDK 依赖） |

---

## 三、项目结构

```
OnlineTest/
├── README.md                 # 快速启动入口
├── INTRO.md                  # 本文档：项目介绍与需求总览
├── PROJECT.md                # 项目总文档：架构、设计、流程、配置
│
├── backend-exam/             # ===== 后端（Spring Boot，端口 8080）=====
│   ├── pom.xml / mvnw        # Maven 构建（自带包装器）
│   ├── upload/               # 轮播图图片落盘目录
│   ├── Request.md            # 接口文档（对接权威）
│   ├── Target.md             # 前端交付文档（页面→接口映射）
│   ├── README.md             # 后端项目说明
│   └── src/main/
│       ├── java/com/example/demo/
│       │   ├── controller/   # 9 个控制器：User/Banner/Notice/Category/
│       │   │                 #   Question/Exam/Record/File/Stats
│       │   ├── service/      # 业务层：组卷抽题、自动判分、Excel、AI 出题
│       │   ├── mapper/       # MyBatis 纯注解接口
│       │   ├── entity/dto/vo/# 表实体 / 入参 / 出参
│       │   ├── common/       # 统一返回 Result、分页 PageResult、全局异常、@RequireAdmin
│       │   ├── config/       # JWT 拦截器、CORS 与静态映射、种子数据初始化
│       │   └── util/         # JwtUtil / JsonUtil / ExcelUtil
│       └── resources/
│           ├── application.yml   # 数据源、JWT、上传目录、LLM Key
│           └── db/schema.sql     # 建表脚本（启动自动执行，幂等）
│
└── frontend-exam/            # ===== 前端（Vue 3 + Vite，端口 5173）=====
    ├── request.md            # 前端需求文档 + 实现状态对照
    ├── README.md             # 前端运行手册
    └── src/
        ├── api/              # 9 个接口模块 + axios 封装（拦截器）
        ├── router/           # 路由表 + 角色守卫
        ├── store/            # Pinia：user（token）/ exam（考试态）/ app
        ├── layout/           # StudentLayout（顶部导航）/ AdminLayout（侧边栏）
        ├── components/       # UploadImage / RichText / AnswerCard
        ├── utils/            # 数据字典枚举、时间与倒计时格式化
        └── views/
            ├── login/        # 登录页
            ├── student/      # 学生端 8 页
            └── admin/        # 管理端 8 页
```

---

## 四、功能需求

### 4.0 通用：登录与鉴权

- 账号密码登录，成功后按 role 跳转对应端首页；登录按钮防重复提交；
- token 持久化（localStorage），刷新页面不掉线；所有请求自动携带；
- token 失效（401）自动清理并跳回登录页；越权访问（403）明确提示；
- 退出登录后清空本地状态。

### 4.1 学生端（前台，8 个页面）

| 页面 | 路由 | 功能需求 |
| :--- | :--- | :--- |
| 首页 | `/home` | 轮播图（仅启用项，可配跳转链接）；最新 5 条公告（类型标签 + 点击进详情）；热门 10 题（按浏览量排序，含难度/题型标签） |
| 公告列表 | `/notice` | 展示全部已发布公告，按类型（通知/警告/活动）打标，点击进详情 |
| 公告详情 | `/notice/:id` | 标题 + 类型 + 发布时间 + 富文本正文 |
| 题库浏览 | `/question` | 按分类（下拉树）/难度/题型/题干关键词组合筛选，分页展示；题目详情弹窗可查看选项，答案与解析折叠展示（点开才可见） |
| 考试列表 | `/exam` | 仅展示已发布试卷：名称、总分、时长、题数；点击进入答题 |
| **考试答题** | `/exam/:id/do` | 进入即开考下发题目（不含答案）；顶部红色倒计时（≤5 分钟闪烁告警），归零自动交卷；全题滚动展示 + 右侧答题卡（题号网格、已答/未答/当前题三色区分、点击跳题）；交卷二次确认（提示未答题数）；交卷后跳转成绩页并自动弹出本次详情；关闭/刷新页面有离开提醒 |
| 我的成绩 | `/record` | 本人全部考试记录：得分、正确/错误数、状态、交卷时间；详情含逐题明细（我的答案 vs 正确答案 + 解析，对错着色）；可删除本人记录 |
| 排行榜 | `/rank` | 全员成绩排行（按总分、正确数降序），前三名奖牌标识；支持按试卷筛选查看单卷排名 |

### 4.2 管理端（后台，8 个页面）

| 页面 | 路由 | 功能需求 |
| :--- | :--- | :--- |
| 数据概览 | `/admin/dashboard` | 7 项总量统计卡片（题目/试卷/已发布/用户/学生/记录/分类）；题目难度分布、各分类题目数两个条形图 |
| 轮播图管理 | `/admin/banner` | 分页列表（缩略图预览）；新增/编辑弹窗含拖拽图片上传（限 10MB）、标题、跳转链接、排序权重；启用/禁用开关即时生效；删除需确认 |
| 公告管理 | `/admin/notice` | 分页列表；新增/编辑弹窗含标题、类型（通知/警告/活动）、富文本正文（加粗/斜体/下划线）；发布/草稿开关即时切换；删除需确认 |
| 分类管理 | `/admin/category` | 列表/树形双视图切换；显示各分类题目数；新增/编辑支持选择父级分类（树形层级）与排序；被题目引用的分类删除时被拒绝并提示 |
| 题目管理 | `/admin/question` | 分类树 + 难度 + 题型 + 关键词组合筛选分页；新增/编辑弹窗：题干、选项动态增删（2~8 个）、正确答案、解析、分类、难度；**AI 生成**：输入知识点 + 数量 + 难度 + 分类，生成结果表格勾选后批量入库；删除需确认 |
| 题目导入 | `/admin/question/import` | 三步流程：下载 Excel 模板（题干/选项A-D/答案/解析/难度/分类）→ 上传解析预览（分页表格核对）→ 确认批量导入（分类名不存在自动创建），完成页显示成功条数 |
| 试卷管理 | `/admin/exam` | 分页列表（总分/时长/题数/状态）；**手动组卷**：弹窗内嵌可筛选题库勾选题目 + 填写名称/总分/时长；**智能组卷**：多条「分类+数量+难度」规则增删，一键抽题成卷；试卷详情弹窗展示全部题目含答案与解析；草稿→发布→停止状态流转（二次确认）；删除需确认 |
| 考试记录管理 | `/admin/record` | 分页查看所有学生的考试记录（含昵称/账号），按学生 ID / 试卷 ID 筛选；任意记录详情（逐题明细）；可删除任意记录 |

### 4.3 后台支撑能力（无独立页面）

- **文件上传**：图片上传返回可访问 URL，供轮播图引用；
- **接口文档**：Knife4j 在线文档（`/doc.html`），全部接口可调试；
- **数据初始化**：首次启动自动建库建表并灌入种子数据（管理员/学生账号各一 + 行测四大分类「言语理解/数量关系/判断推理/资料分析」+ 12 道样例题目）。

---

## 五、数据模型（7 张表）

| 表 | 职责 | 要点 |
| :--- | :--- | :--- |
| `sys_user` | 用户 | 账号唯一，密码 BCrypt 加密，role 区分管理员/学生 |
| `banner` | 轮播图 | 图片地址、跳转链接、排序权重、启用状态 |
| `notice` | 公告 | 类型（通知/警告/活动）、状态（草稿/发布）、富文本内容 |
| `category` | 题目分类 | parent_id 支持树形层级，删除前校验题目引用 |
| `question` | 题目 | 题干、选项（JSON 数组）、标准答案、解析、难度（1-3）、浏览量（热门排序依据） |
| `exam` | 试卷 | 题目 ID 列表（JSON 数组）、总分、时长、状态（草稿/发布/结束） |
| `exam_record` | 考试记录 | 考生答案（JSON 对象）、得分、正确/错误数、状态（进行中/已交卷）、起止时间 |

**关键业务规则**：

1. 试卷状态流转：草稿(0) → 已发布(1) → 已结束(2)，仅「已发布」可被学生开考；
2. 判分：每题分值 = 总分 ÷ 题目数，逐题对照标准答案，交卷即出分且不可重复提交；
3. 权限：管理端全部写接口校验 role=0；考试记录详情/删除校验本人归属（管理员通道除外）；
4. 热门题目按浏览量降序，查看详情浏览量 +1。

---

## 六、核心业务流程

```
【管理员】备题（手动录入 / Excel 导入 / AI 生成勾选入库）
        → 组卷（手动选题 或 规则智能抽题）→ 草稿 → 发布
【学生】  考试列表 → 开始考试（倒计时启动）→ 逐题作答 → 交卷（自动判分）
        → 成绩详情（逐题对错 + 解析）→ 排行榜
【管理员】数据概览监控全局 → 考试记录管理查看/筛选任意考生明细
```

---

## 七、运行环境与已知边界

**环境要求**：JDK 21、MySQL 8.0、Node.js ≥ 20；DeepSeek API Key（可选，仅 AI 出题需要）。

**已知边界**：

1. AI 出题需在后端配置真实 `llm.api-key`，未配置时该功能明确报错，不影响其余功能；AI 生成为同步调用，耗时约 10~60 秒；
2. 考试无续考机制：答题中刷新页面会丢失进度，需重新开考；
3. 排行榜的按卷筛选为前端本地过滤，「用时」因接口未返回开始时间而不展示；
4. Excel 模板仅支持单选题导入。

---

## 八、文档导航

| 想了解 | 看这份 |
| :--- | :--- |
| 快速跑起来 | [`README.md`](README.md) |
| 架构、前后端设计详述、配置约定 | [`PROJECT.md`](PROJECT.md) |
| 接口字段级对接细节 | [`backend-exam/Request.md`](backend-exam/Request.md) |
| 前端页面→接口映射与交付清单 | [`backend-exam/Target.md`](backend-exam/Target.md) |
| 后端结构与算法说明 | [`backend-exam/README.md`](backend-exam/README.md) |
| 前端需求与实现差异对照 | [`frontend-exam/request.md`](frontend-exam/request.md) |
| 前端运行手册 | [`frontend-exam/README.md`](frontend-exam/README.md) |
